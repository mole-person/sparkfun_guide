# Guides

This folder contains the eight deliverables from `PROJECT_BRIEF.md`.

Build these files from the local source archive in `docs/`. If a procedure is uncertain, mark the uncertainty and cite the source that needs verification.
